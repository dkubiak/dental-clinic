package com.dentalclinic.patient.toothchart;

/**
 * Fixed at {@link ToothPosition} creation time — tells the frontend which positions a given {@link
 * DentitionMode} should render (data-model.md).
 */
public enum DentitionType {
  PERMANENT,
  DECIDUOUS
}
