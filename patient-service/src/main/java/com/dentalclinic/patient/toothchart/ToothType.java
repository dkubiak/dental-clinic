package com.dentalclinic.patient.toothchart;

/** Derived once from {@code fdiNumber} at seed time — drives crown/root geometry parameters
 * (research.md D11) and which surfaces exist for a position (FR-024). */
public enum ToothType {
  INCISOR,
  CANINE,
  PREMOLAR,
  MOLAR
}
