package com.dentalclinic.patient.toothchart;

/** FR-021 — the closed set of anatomical scopes a catalog entry can declare. */
public enum AnatomicalScope {
  SURFACE,
  WHOLE_TOOTH,
  ROOT_PERIAPICAL,
  PERIODONTIUM
}
