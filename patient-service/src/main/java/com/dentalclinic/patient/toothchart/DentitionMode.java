package com.dentalclinic.patient.toothchart;

/** FR-043 — the chart-level view filter over the 52 positions that always exist (research.md D2). */
public enum DentitionMode {
  PERMANENT,
  DECIDUOUS,
  MIXED
}
