package com.dentalclinic.patient.toothchart;

/**
 * FR-058 — a snapshot of the role the author acted in at write time, independent of that account's
 * role today.
 */
public enum FindingAuthorRole {
  DOCTOR,
  ASSISTANT
}
